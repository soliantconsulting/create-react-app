# {{project.title}}

## Setup

- `pnpm install`
- `pnpm start`

## Favicons

This project automatically generates favicons and injects them into the index.html file during build. To change the
existing favicon, simply replace `src/assets/favicon.svg` with another SVG file.
